import { IMAGES } from './images';

// Re-export content from content.ts to maintain backward compatibility with components
export * from './content';

export const DEC_COLORS = {
  primary: '#F58220', // Orange from logo
  secondary: '#808080', // Grey from logo
  dark: '#0a0a0a',
  light: '#ffffff',
};

export const LOGO_URL = IMAGES.LOGO;